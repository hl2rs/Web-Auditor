# ---------------------------------------------------------------------------
# Auditor - Python dependencies
# ---------------------------------------------------------------------------
# Core web framework
Flask==3.0.3

# Headless browser automation for JS rendering + console/network bug capture
# 1.48.0 pins greenlet==3.1.1, which ships prebuilt wheels for Python 3.13
playwright==1.48.0

# Fast HTML parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# Asynchronous HTTP client used by the link checker
aiohttp==3.10.5

# PDF report generation
reportlab==4.2.2

# Production WSGI server (optional but recommended)
waitress==3.0.0
